package editor.model.map;

import java.io.File;

/**
 * Saves an Map into the file.
 */
public class MapSaver {

    /** File to save to. */
    private File mapFile;

    /**
     * @param file to save to
     */
    MapSaver(final File file) {
        this.mapFile = file;
        //TODO Saver bauen
    }

    /**
     * saves the map into the file.
     * @param map to save
     */
    public void save(final Map map) {

    }
}
